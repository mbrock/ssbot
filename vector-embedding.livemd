# vector embedding stuff

## Section

```elixir
index = ExFaiss.Index.new(1536, "Flat")
```

```elixir
{:ok, ecar} = NodeTown.gpt3_embedding("car")
{:ok, edog} = NodeTown.gpt3_embedding("dog")
{:ok, epuppy} = NodeTown.gpt3_embedding("puppy")

all = [ecar, edog, epuppy] |> Enum.map(&Nx.tensor/1)
```

```elixir
index = Enum.reduce(all, index, &ExFaiss.Index.add(&2, &1))
```

```elixir
ExFaiss.Index.get_num_vectors(index)
```

```elixir
{:ok, evovve} = NodeTown.gpt3_embedding("CIA")
ExFaiss.Index.search(index, evovve, 3)
```
